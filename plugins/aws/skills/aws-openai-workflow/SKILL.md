---
name: aws-openai-workflow
description: Backward-compatible alias. Use `aws-workflow` for current AWS workflow guidance.
---

This skill name is kept for backward compatibility.

Use the primary workflow instead:

- `aws-workflow` at `skills/aws-workflow/SKILL.md`
